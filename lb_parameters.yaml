#
# openstack stack create -e haproxy_paramters.yaml -e rhn_credentials.yaml -t haproxy.yaml lb-service
#
parameters:
  #image: rhel7
  #flavor: m1.small
  #ssh_user: cloud-usera

  image: fedora25
  flavor: m1.tiny
  ssh_user: fedora

  external_network: public_network
  service_network: dns-network
  service_subnet: dns-subnet

  hostname: lb
  domain_name: osp10.e2e.bos.redhat.com
  ssh_key_name: ocp3
